<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .container {
            width: 80%;
            margin: 70px auto;
        }

        h2 {
            background-color: lightgray;
            margin-left: 25px;
            border-radius: 5px;
            padding: 5px 20px 5px 20px;
            width: 50%;
        }

        h4 {
            margin-bottom: 5px;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        tr th {
            background-color: rgb(189, 228, 247);
            text-align: left;
        }

        th,
        td {
            padding: 10px;
            border: 1px solid rgb(232, 232, 232);
        }

        .take, .remove {
            text-decoration: none;
            display: inline-block;
            padding: 2px 10px;
            border-radius: 3px;
            font-size: 15px;
            color: white
        }

        .take {
            background-color: lightgreen;
        }

        .remove {
            background-color: lightgray;
        }
    </style>
</head>

<body>
    <%- include('../partials/navbar', {title: 'courses', id}) %>
        <div class="container">
            <h4>All Courses<span style="float: right; color: red; font-size: 10px;">
                <%= errors %>
            </span></h4>
            <table>
                <tr>
                    <th>Course Name</th>
                    <th>Lecturer</th>
                    <th>Credits</th>
                    <th>Action</th>
                </tr>

                <% for (let i=0; i < courses.length; i++) { %>
                    <tr>
                        <td>
                            <%= courses[i].course_name %>
                        </td>
                        <td>
                            <%= courses[i].lecturer %>
                        </td>
                        <td>
                            <%= courses[i].credits %>
                        </td>
                        <td>
                            
                            <a class="take" href="/students/take?id=<%= id %>&course_id=<%= courses[i].id %>">Take</a>
                        </td>
                    </tr>
                    <% } %>

            </table>
        </div>
</body>

</html>   